//some function in js
<script>
        var ages = [12, 34, 22];
        var b = ages.some(checkadult);
        document.write(b + "<br> <br> ");

        function checkadult(age) {
            return age >= 18;
        }

        //ar ages =[20,22,3    we need the place whare you are gettinsion of o
    </script>
